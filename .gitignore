# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Node (if you ever add deps)
node_modules/
npm-debug.log*

# Netlify
.netlify/

# Local env (never commit secrets)
.env
.env.local
.env.*.local

# Local-only verifiers / scratch (e.g. tools/.audit-meals-verify.* used when
# Node was unavailable during T-003; the JS in tools/ is the canonical version)
tools/.*
